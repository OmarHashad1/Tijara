export enum ROLE {
  USER = 'user',
  ADMIN = 'admin',
}

export enum USER_STATUS {
  ACTIVE = 'active',
  DEACTIVATED = 'deativated',
  BANNED = 'BANNED',
}


export enum PAYMENT_METHOD {
  CARD = 'card',
  POD = 'POD',
}
