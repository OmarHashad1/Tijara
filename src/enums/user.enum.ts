export enum ROLE {
  USER = "user",
  COMPANY = "company",
  ADMIN = "admin",
}

export enum PROVIDER {
  SYSTEM = "system",
  GOOGLE = "google",
}


export enum USER_STATUS {
  ACTIVE = "active",
  DEACTIVATED = "deativated",
  BANNED = "BANNED",
}

export enum LOGOUT_TYPE {
  ALL = "all",
  DEVICE = "device",
}
